const NotificationBar = () => {
  return (
    <div className="w-full bg-gradient-to-r from-primary/90 via-primary to-primary/90 py-2.5 px-4">
      <p className="text-center text-primary-foreground text-xs md:text-sm tracking-[0.15em] uppercase font-medium">
        First Batch Pre-Orders Close 20.12 — Save €100
      </p>
    </div>
  );
};

export default NotificationBar;
